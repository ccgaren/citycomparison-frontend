import { createAction } from 'redux-act'

export const search = createAction();
export const saveCities = createAction();
export const changeCityA = createAction();
export const changeCityB = createAction();
export const receiveCityData = createAction();
export const receiveSearchData = createAction();
export const receiveUserSavedCitiesData = createAction();